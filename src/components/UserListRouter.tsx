import React, { useState } from 'react';

interface IState {
    count : number;
}
interface IProps {}

let UserListRouter:React.FC<IProps> = () => {

    return (
        <React.Fragment>
            <h3>UserListRouter</h3>
        </React.Fragment>
    );
};

export default UserListRouter;